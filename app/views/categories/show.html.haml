#map_canvas{:style => "width: 500px; height: 500px"}
  = image_tag 'loading.gif', :style => 'margin-top: 200px'

%h2 Category: #{@category.name}
= render :partial => 'shared/restaurant_table'

:javascript
  var restaurants = #{@restaurants.to_json};
  var x = #{@location.x_center};
  var y = #{@location.y_center};
  var zoom = #{@location.zoom_level};

  $("#restaurant_table").tablesorter();
  $(document).ready(function() {
    initialize();
  });
= javascript_include_tag 'http://maps.google.com/maps/api/js?sensor=false'
